/* Escreva um programa que gera e escreve os quatro primeiros numeros perfeitos. Um numero perfeito é aquele que é igual a soma dos seus divisores (por exemplo 6,28, etc) */

#include <stdio.h>
#include <stdlib.h> /* Para usar o atoi */

int main(int argc, char **argv){
	int contador = atoi(argv[1]); /* atoi converte para inteiro */
	int ocorrencias = 0;

	while(ocorrencias < 4){
		for(int i = 1; soma = 0 i < )
		
	}


	return 0;	
}
